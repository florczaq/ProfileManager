#pragma once
#include <iostream>

using std::string;

class Descriptions
{
public:
  const string AppDescription = "Profile Manager CLI - Interface in command line, that helps with your daily work.";
  const string ProfilesSubcommand=  "Manage profiles. Leave with no flag to get profiles list";
};